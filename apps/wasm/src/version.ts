// =============================================================================
// Cells version (web UI)
// =============================================================================
// Keep in sync with MODULE.bazel and apps/cli/cli_version.h default.

/** Product version string (no leading "v"). */
export const CELLS_VERSION = "0.0.1";
